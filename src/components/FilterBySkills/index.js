import React from 'react';

const FilterBySkills = ({
  projectId,
  skills,
  addSkillFilter,
  removeSkillFilter
}) => {
  return (
    <div>
      ContributionFilter
    </div>
  );
};

export default FilterBySkills;